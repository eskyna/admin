{
  "name": "eskyna-social-media-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Password-protected Hugo workspace for ESKYNA social media assets and brand documentation.",
  "scripts": {
    "dev": "hugo server --baseURL http://localhost:1313/admin/ --appendPort=false --navigateToChanged",
    "build": "bash scripts/build.sh",
    "build:protected": "bash scripts/build-protected.sh"
  },
  "devDependencies": {
    "staticrypt": "3.5.4"
  }
}
